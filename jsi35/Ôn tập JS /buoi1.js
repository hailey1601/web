// Hàm prompt: hiện ra ô input để khai báo dữ liệu

let ten_nyc = 'trang'

// Nếu mà chỉ có 2 kết quả xảy ra thì ta chỉ cần dùng if và else
/* if (điều kiện) {
    chỗ này được thực thi thì điều kiện đúng 
} else {
    chỗ này được thực thi khi điều kiện sai 
}
*/ 

/* if (điều kiện 1) {
    chỗ này được thực thi thì điều kiện 1 đúng 
} else if (điều kiện 2) {
    chỗ này được thực thi khi điều kiện 2 đúng 
} else if (điều kiện 3) {
    chỗ này được thực thi khi điều kiện 3 đúng 
} else {
    chạy khi không điều kiện nào bên trên đúng
}
*/ 

if (ten_nyc == "trang") {
    console.log("Người Đức đã từng yêu");
} else {
    console.log("Đức chưa từng yêu người này");
}

